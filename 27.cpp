#include<bits/stdc++.h>

using namespace std;

struct node {
    struct node * father;
    struct node * lchild;
    struct node * rchild;
    int data;
};

int main(){
    int input;
    scanf("%d",&input);
    struct node * head = (struct node *)malloc(sizeof(struct node));
    for(;;){
        
    }
    return 0;
}